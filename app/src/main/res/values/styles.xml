<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">#1A2A6C</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <style name="defaultToolbarTheme" parent="Theme.MaterialComponents.NoActionBar">
        <item name="popupMenuBackground">@drawable/item_background_menu</item>
    </style>

    <style name="OverflowMenuStyle" parent="Widget.MaterialComponents.PopupMenu.Overflow">
        <item name="android:popupBackground">@null</item>
    </style>

    <style name="CustomAlertDialog" parent="Theme.MaterialComponents.DayNight.Dialog.Alert">
        <!-- Customize the title color -->
        <item name="android:textColorPrimary">@color/black</item> <!-- or your preferred color -->
        <!-- Customize other dialog properties like background or button styles if needed -->
    </style>

    <style name="CalculatorButtonStyle">
        <item name="android:textSize">25sp</item>
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">0dp</item>
        <item name="android:gravity">center</item>
        <item name="android:background">@drawable/calculator_button_background</item>
        <item name="android:layout_margin">4dp</item>
    </style>

</resources>